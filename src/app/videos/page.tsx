import { RoutePlaceholder } from "@/components/route-placeholder";

export default function VideosPage() {
  return (
    <RoutePlaceholder
      description="The video library route is ready. Upload, indexing, and demo data arrive in later phases."
      eyebrow="Video library"
      title="Your learning videos will live here."
    />
  );
}
