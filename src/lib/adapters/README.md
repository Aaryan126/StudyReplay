# Integration adapters

Sponsor-specific implementations will live here behind internal interfaces.
Phase 0 intentionally provides structure only; mock adapters begin in the
integration phases defined by `plan.md`.
